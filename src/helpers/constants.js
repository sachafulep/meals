const screens = { splash: 1, selectHousehold: 2, createHousehold: 3, home: 4 };
const modals = { insertMealModal: 1 };

const constants = new Map();
constants.set("screens", screens);
constants.set("modals", modals);

export default constants;
